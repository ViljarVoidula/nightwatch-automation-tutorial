
const tutorial = ['Meg', 'Steven', 'Brian', 'Marcus'];
console.log('This is an array: ', tutorial);
// array indexes:

// console.log('I will log out third element of array , index starts with 0 position: ', tutorial[2])

// We want to know our array length

// console.log(`Tutorial array has ${tutorial.length} elements, but we want to add another to this!`);

// tutorial.push('Student');

// console.log(`The array of ${JSON.stringify(tutorial)} has now "${tutorial[4]}" added there to the end.`);

// other important methods to remember

// pop - Removes from the End of an Array
// shift - Removes from the beginning of an Array
// splice - removes from a specific Array index
// filter - allows you to programatically remove elements from an Array

// const newArray = tutorial.filter((element)=>{
//     return element.match(/M/)
// })

// console.log('All array to match pattern', newArray);